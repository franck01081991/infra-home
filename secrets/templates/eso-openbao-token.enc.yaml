# Template non chiffré pour le token ESO ↔ OpenBao.
# Chiffrer avec `sops -e secrets/templates/eso-openbao-token.enc.yaml > secrets/eso-openbao-token.enc.yaml`.
apiVersion: v1
kind: Secret
metadata:
  name: eso-openbao-token
  namespace: security
stringData:
  token: "change-me"
