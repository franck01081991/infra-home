# Template non chiffré : exécuter `sops -e secrets/templates/wpa_supplicant.env.enc.yaml > secrets/wpa_supplicant.env.enc.yaml`
# puis supprimer ce fichier en clair. Les valeurs ci-dessous sont des placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: wpa-supplicant-env
  namespace: kube-system
stringData:
  wpa_supplicant.env: |
    WAN_4G_PSK="change-me"
    INFRA_K3S_PSK="change-me"
